# SSH Log Parser & Security Event Normalizer

A Python script that parses raw Linux SSH authentication logs and converts failed login attempts into structured JSON data.

## Tech Stack

- Python 3
- JSON

## Project Structure

```text
ssh-log-parser/
├── logs/
│   └── sample_auth.log
├── parser.py
├── output.json
└── README.md
